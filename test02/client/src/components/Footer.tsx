import React from 'react';
import styled from 'styled-components';

const StyledFooter = styled.div`
  border-top: 2px solid #003b58
  margin-top: 1rem;
`;

const Footer = ({ title }) => {
  return (
    <StyledFooter>
    </StyledFooter>
  );
};

export default Footer;
